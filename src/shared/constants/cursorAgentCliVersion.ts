/**
 * Browser-safe Cursor Agent CLI build pin.
 *
 * OAuth metadata is also used by the dashboard model catalog, so it must not
 * import the filesystem-backed version resolver.
 */
export const CURSOR_AGENT_CLI_VERSION = "2026.07.08-0c04a8a";
